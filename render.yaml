services:
  - type: web
    name: continuity-protocol
    runtime: node
    rootDir: continuity-protocol
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
